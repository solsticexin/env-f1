# 基于 stm32f103c8t6 的环境检测系统

本文档详细说明项目中三个主要模块的工作原理、硬件连接、初始化流程、MCU 如何接收与存储数据，以及使用和调试要点：

- `dh11.rs`：DHT11 温湿度传感器驱动
- `lcd.rs`：基于 ST7735 的 SPI LCD 驱动与封装
- `soil_sensor.rs`：基于 ADC 的土壤湿度读取辅助库

下面逐个模块详细说明。

## dh11.rs（DHT11 温湿度传感器驱动）

概述：
dh11.rs 提供了一个阻塞（blocking）风格的 DHT11 传感器驱动，使用单线数字协议与传感器通信。为避免浮点运算，读取的温度和湿度以"十分之一"单位（tenths）存储，例如 253 表示 25.3°C。

工作原理：
- DHT11 使用单线半双工协议。主机（MCU）通过 GPIO 发出启动信号（长低电平），传感器响应后发送 40 位数据：湿度整数、湿度小数、温度整数、温度小数、校验和。
- 每个数据位由一个固定约 50µs 的低电平起始，再跟随一个高电平：高电平持续约 26µs 表示 0，约 70µs 表示 1。驱动通过测量高电平时长来区分 0/1。

硬件连接（典型）：
- 信号线接 GPIO（要求开漏或可模拟开漏的配置），MCU 端需要上拉电阻（通常 4.7kΩ）。
- VCC 接 5V 或 3.3V（与板子电源一致），GND 共地。

初始化（代码层面）：
- 构造函数 `Dht11::new(pin)`：将 GPIO 拉高以保证总线处于空闲高电平。

读取流程（高层）：
1. start_signal：主机将数据线拉低至少 18ms（驱动里 20ms），然后拉高并等待传感器准备（约 20-40µs）。
2. await_sensor_response：传感器先拉低约 80µs、再拉高约 80µs、再拉低，表明进入发送阶段。
3. 对 40 位循环：对每位，先等待传感器拉低（起始 ~50µs），再等待拉高并测量高电平持续时间，通过阈值（代码中约 40µs）判定 0/1。
4. 校验和验证：接收到 5 字节后，将前 4 字节相加并与第 5 字节比较，若不匹配返回 `Error::Checksum`。

代码要点与实现细节：
- 使用了 `DelayNs` trait（通常由 HAL 的 SysTick-delay 提供）进行微秒/毫秒级延时。
- 错误类型 `Error` 包含 `Timeout`（超时）、`Checksum`（校验失败）与 `Pin(PinError)`（GPIO 访问错误）。
- 返回值 `Reading` 将湿度和温度以十分之一单位保存（例如湿度 45.6% -> 456）。驱动里把两字节组合为 tenths：`humidity = data[0]*10 + data[1]`。

边界条件与注意事项：
- 传感器的时序对延时精度敏感，使用不准确的延时实现可能导致读取失败或位判别错误。
- DHT11 比较慢，通常一次读取需 > 5–10 ms，总体阻塞时间更长（包含启动、响应与 40 位传输），不适合高频轮询。
- 当环境噪声或线路长时，建议加滤波或重复读取并取中值。
- 若 MCU GPIO 不是开漏，请确保有上拉并在发送低时配置为推挽输出再切回输入（本驱动假定调用方提供可读写同一引脚的类型）。

如何存储与上层接口：
- 驱动返回 `Reading { humidity_tenths, temperature_tenths }`，上层可以直接将这两个 u16 存入环形缓冲、日志（flash）或通过串口/USB 发送到主机。
- 建议的存储格式（示例二进制）：
	- 2 字节：湿度（u16, tenths, 小端）
	- 2 字节：温度（u16, tenths, 小端）
	- 4 字节：UNIX 时间戳（可选）

示例（伪代码）：
- 调用 `let mut dht = Dht11::new(pin)?; let reading = dht.read(&mut delay)?;` 然后保存 `reading.humidity_tenths` 与 `reading.temperature_tenths`。

调试技巧：
- 在驱动中已加入 `defmt::info!` 输出原始数据，这对观察传感器返回字节很有帮助。

## lcd.rs（ST7735 SPI LCD 驱动）

概述：
lcd.rs 用于控制基于 ST7735 控制器的彩色 TFT LCD（128x160）。工程中使用 `st7735_lcd` crate，并对 SPI 设备做了一层包装以便手动管理 CS（片选）引脚。

硬件连接与信号：
- MOSI -> MCU MOSI（代码示例为 PA7）
- SCK  -> MCU SCK（PA5）
- CS   -> 片选（PA4）
- DC   -> 数据/命令选择（PA2，低表示命令，高表示数据）
- RST  -> 复位（PA3）
- BL   -> 背光（PB9）

SPI 与 CS 管理：
- HAL 的 SPI 抽象提供的是裸 SPI 总线，但 `st7735_lcd` 期望一个实现了 `SpiDevice` trait 的设备。为此实现了 `SpiDeviceWithCs<SPI, CS>`：
	- 在 `transaction` 方法中，调用方在每次事务开始时将 CS 拉低（选中），事务结束后拉高（释放）。
	- 该实现处理 `Operation::Read/Write/Transfer` 等操作并在发生 `DelayNs` 操作时调用本地忙等待 `busy_wait_ns`。

显示初始化流程（代码中实现）：
1. 在 `init` 函数中，首先将 CS 拉高（空闲），并关闭背光以避免闪烁。
2. 配置 SPI：在 `spi_peripheral.spi(...)` 中设置所需的模式、时钟频率（示例使用 16MHz）、和引脚。
3. 创建 `ST7735` 实例并调用 `display.init(delay)` 完成控制器寄存器初始化。
4. 设置显示方向、偏移等（例如 `Portrait` 和偏移 0,0）。
# 基于 stm32f103c8t6 的环境检测系统

本文档详细说明项目中三个主要模块的工作原理、硬件连接、初始化流程、MCU 如何接收与存储数据，以及使用和调试要点：

- `dh11.rs`：DHT11 温湿度传感器驱动
- `lcd.rs`：基于 ST7735 的 SPI LCD 驱动与封装
- `soil_sensor.rs`：基于 ADC 的土壤湿度读取辅助库

下面逐个模块详细说明。

## dh11.rs（DHT11 温湿度传感器驱动）

概述：

dh11.rs 提供了一个阻塞（blocking）风格的 DHT11 传感器驱动，使用单线数字协议与传感器通信。为避免浮点运算，读取的温度和湿度以“十分之一”单位（tenths）存储，例如 253 表示 25.3°C。

工作原理：

- DHT11 使用单线半双工协议。主机（MCU）通过 GPIO 发出启动信号（长低电平），传感器响应后发送 40 位数据：湿度整数、湿度小数、温度整数、温度小数、校验和。
- 每个数据位由一个固定约 50µs 的低电平起始，再跟随一个高电平：高电平持续约 26µs 表示 0，约 70µs 表示 1。驱动通过测量高电平时长来区分 0/1。

硬件连接（典型）：

- 信号线接 GPIO（要求开漏或可模拟开漏的配置），MCU 端需要上拉电阻（通常 4.7kΩ）。
- VCC 接 5V 或 3.3V（与板子电源一致），GND 共地。

初始化（代码层面）：

- 构造函数 `Dht11::new(pin)`：将 GPIO 拉高以保证总线处于空闲高电平。

读取流程（高层）：

1. start_signal：主机将数据线拉低至少 18ms（驱动里 20ms），然后拉高并等待传感器准备（约 20–40µs）。
2. await_sensor_response：传感器先拉低约 80µs、再拉高约 80µs、再拉低，表明进入发送阶段。
3. 对 40 位循环：对每位，先等待传感器拉低（起始 ~50µs），再等待拉高并测量高电平持续时间，通过阈值（代码中约 40µs）判定 0/1。
4. 校验和验证：接收到 5 字节后，将前 4 字节相加并与第 5 字节比较，若不匹配返回 `Error::Checksum`。

代码要点与实现细节：

- 使用了 `DelayNs` trait（通常由 HAL 的 SysTick-delay 提供）进行微秒/毫秒级延时。
- 错误类型 `Error` 包含 `Timeout`（超时）、`Checksum`（校验失败）与 `Pin(PinError)`（GPIO 访问错误）。
- 返回值 `Reading` 将湿度和温度以十分之一单位保存（例如湿度 45.6% -> 456）。驱动里把两字节组合为 tenths：`humidity = data[0]*10 + data[1]`。

边界条件与注意事项：

- 传感器的时序对延时精度敏感，使用不准确的延时实现可能导致读取失败或位判别错误。
- DHT11 比较慢，通常一次读取需 > 5–10 ms，总体阻塞时间更长（包含启动、响应与 40 位传输），不适合高频轮询。
- 当环境噪声或线路长时，建议加滤波或重复读取并取中值。
- 若 MCU GPIO 不是开漏，请确保有上拉并在发送低时配置为推挽输出再切回输入（本驱动假定调用方提供可读写同一引脚的类型）。

如何存储与上层接口：

- 驱动返回 `Reading { humidity_tenths, temperature_tenths }`，上层可以直接将这两个 u16 存入环形缓冲、日志（flash）或通过串口/USB 发送到主机。
- 建议的存储格式（示例二进制）：
  - 2 字节：湿度（u16, tenths, 小端）
  - 2 字节：温度（u16, tenths, 小端）
  - 4 字节：UNIX 时间戳（可选）

示例（伪代码）：

- 调用 `let mut dht = Dht11::new(pin)?; let reading = dht.read(&mut delay)?;` 然后保存 `reading.humidity_tenths` 与 `reading.temperature_tenths`。

调试技巧：

- 在驱动中已加入 `defmt::info!` 输出原始数据，这对观察传感器返回字节很有帮助。

## lcd.rs（ST7735 SPI LCD 驱动）

概述：

lcd.rs 用于控制基于 ST7735 控制器的彩色 TFT LCD（128x160）。工程中使用 `st7735_lcd` crate，并对 SPI 设备做了一层包装以便手动管理 CS（片选）引脚。

硬件连接与信号：

- MOSI -> MCU MOSI（代码示例为 PA7）
- SCK  -> MCU SCK（PA5）
- CS   -> 片选（PA4）
- DC   -> 数据/命令选择（PA2，低表示命令，高表示数据）
- RST  -> 复位（PA3）
- BL   -> 背光（PB9）

SPI 与 CS 管理：

- HAL 的 SPI 抽象提供的是裸 SPI 总线，但 `st7735_lcd` 期望一个实现了 `SpiDevice` trait 的设备。为此实现了 `SpiDeviceWithCs<SPI, CS>`：
  - 在 `transaction` 方法中，调用方在每次事务开始时将 CS 拉低（选中），事务结束后拉高（释放）。
  - 该实现处理 `Operation::Read/Write/Transfer` 等操作并在发生 `DelayNs` 操作时调用本地忙等待 `busy_wait_ns`。

显示初始化流程（代码中实现）：

1. 在 `init` 函数中，首先将 CS 拉高（空闲），并关闭背光以避免闪烁。
2. 配置 SPI：在 `spi_peripheral.spi(...)` 中设置所需的模式、时钟频率（示例使用 16MHz）、和引脚。
3. 创建 `ST7735` 实例并调用 `display.init(delay)` 完成控制器寄存器初始化。
4. 设置显示方向、偏移等（例如 `Portrait` 和偏移 0,0）。
5. 初始化完成后把背光置高使屏幕点亮。

代码要点与实现细节：

- `Display` 类型别名为 `ST7735<SpiDeviceWithCs<..., CsPin>, DcPin, RstPin>`，表示将自定义的 SPI 设备与 DC、RST 引脚一起传给 `st7735_lcd`。
- `SpiDeviceWithCs::transaction` 内部会在执行 SPI 操作前把 CS 拉低，操作完成后把 CS 拉高，并在操作间处理 `DelayNs`。
- `busy_wait_ns` 使用了 `cortex_m::asm::nop()` 做粗略延时；这是很粗糙的实现，只适用于非常短且非精确的延时需求。对于精确纳秒级延时应依赖硬件定时器或 HAL 的延时提供者。

使用与注意事项：

- 在调用 `init` 时需要传入正确类型的 GPIO 引脚（见函数签名），并传入 `rcc` 和 `delay` 用于配置和初始化硬件。
- 如需在运行时绘制文本/图形，直接操作 `Display` 实例的绘图 API 即可（`st7735_lcd` 提供 draw_... 方法）。
- 若出现花屏或通信错误，检查：CS/DC/RST/BL 的接线、SPI 模式、时钟速率，和 MOSI/SCK 引脚配置（推挽/复用）。

扩展与改进建议：

- 将 `busy_wait_ns` 用更可靠的定时器替代，以支持更准确的 `DelayNs` 操作。
- 如果需要同时挂多个 SPI 从设备，`SpiDeviceWithCs` 模式很方便：为每个设备创建独立 CS 管理封装。

## soil_sensor.rs（土壤湿度 ADC 采集）

概述：

soil_sensor.rs 提供对接到 PA0（ADC 通道）的土壤电导/电压探针的读取封装。驱动用 12-bit ADC 采样，返回原始计数并提供将计数映射为 0–100% 的 Helper。

硬件连接：

- 土壤探针连接到 `PA0`，作为模拟电压源。探针另一端接地，共地。
- ADC 供电与 MCU VREF 应一致（通常 3.3V），以保证量程解释正确。

ADC 初始化与采样：

- 构造器 `SoilSensor::new(adc, pin)` 会为 ADC 设置较长的采样时间 `SampleTime::T_239`，提高高阻抗探针的稳定性。
- 读取使用阻塞式 `OneShot` API：`block!(self.adc.read(&mut self.pin))`，返回 0–4095（12 位）范围的原始值。

数值转换与存储：

- 常量 `ADC_MAX_COUNT = 4095` 表示 12-bit 的最大值。
- `raw_to_percent(raw)` 将原始计数线性映射为百分比，且做了夹紧：先按干燥百分比计算（raw / 4095），再取补数以获得湿度百分比（0 表示最干，100 表示最湿）。
- 推荐存储格式（示例二进制）：
  - 2 字节：raw u16（小端）
  - 1 字节：percent u8
  - 4 字节：UNIX 时间戳（可选）

边界条件与校准建议：

- 传感器/探针与土壤类型差异很大，线性映射仅为近似。建议在系统部署时标定：记录干土（空气）和水中或饱和土壤对应的 ADC 值，然后按区间重新缩放/非线性映射以提高准确性。
- 对于高阻抗探针，较长的采样时间能减少噪声，但会增加每次读取时间。
- 建议对连续读取施加去抖或滤波（移动平均或指数平滑），并限制读取频率以延长探针寿命和降低功耗。

示例使用：

- 初始化：
  - 在主函数中创建 `Adc` 和将 PA0 切换为 `Analog`，然后 `let mut soil = SoilSensor::new(adc, pa0);`。
  - 读取原始值：`let raw = soil.read_raw()?;`，转换百分比：`let pct = SoilSensor::raw_to_percent(raw);`。

----

附：通用建议

- 在数据存储层（无论是闪存、SD 卡还是通过串口发送）都建议将每次采样记录成结构化记录（时间戳 + 传感器类型 + 数值），便于后续分析。
- 若打算长期记录，设计循环写入闪存时请考虑擦写寿命并使用 wear leveling 或将高频采样数据缓冲并批量写入。

完成：本文件已加入对 `dh11.rs`、`lcd.rs` 与 `soil_sensor.rs` 的详细说明，涵盖工作原理、初始化、MCU 接收/存储方法、代码要点与调试建议。如需我把这些说明转换为项目 README 的独立英文版，或把内容部分提取到源代码注释中，我可以继续处理。

